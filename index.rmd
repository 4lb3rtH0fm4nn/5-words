---
title: "Five Words Analytics"
output: 
  flexdashboard::flex_dashboard:
    storyboard: true
---
```{r}
urltodata<-"/home/webadmin/R/dtm-test/data/"
```
### These are the most common words that popped into your heads

```{r}
#data=read.csv(input, header=T,sep=";")
library(wordcloud)
sumv<-read.table(paste0(urltodata,"cwsum"), header=T,sep="")
commonality.cloud(sumv,comonality.measure=min,scale=c(3.5,0.1),random.order=F, colors = brewer.pal(6, "PuBuGn"),use.layout.r=F)


```


### Here you can see a wordcloud that represents the most frequent words based on gender. Legend: Blue - Male; Red - female; Purple - Other

```{r}
gsum2<-read.table(paste0(urltodata,"gsum"), header=T,sep="")
gsum2<-as.matrix(gsum2)
colnames(gsum2)<-NULL
comparison.cloud(gsum2,scale=c(3,.01),max.words=300,colors=c("deepskyblue3","firebrick2","darkorchid3"))

```


### You can see a cluster dendrogram here

```{r}
library(cluster)
library(ape)


traindtm<-read.table(paste0(urltodata,"dtm"), header=T,sep="")
d<-dist(traindtm)
hc <- hclust(d,method="ward.D")
dtmdim<-dim(traindtm)
size<-log(dtmdim[1],base=exp(10))
plot(hc, cex = size,main="",xlab="",ylab="",sub="")

```